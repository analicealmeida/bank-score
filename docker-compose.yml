version: '3.9'

services:
  mongo:
    image: mongo
    ports:
      - "27017:27017"
  score-service:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8081:8081"
    depends_on:
      - mongo
    environment:
      SPRING_DATA_MONGODB_URI: mongodb://mongo:27017/score
